<%= form_for(@picture, url: url) do |f| %>

  <% if @picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@picture.errors.count, "error") %> prohibited this pircture from being saved:</h2>

      <ul>
      <% @picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Order %><br>
    <%= f.text_field :order, value: 0 %>
  </div>
  <div class="field">
    <%= f.label :Cover %><br>
    <%= f.check_box :cover %>
  </div>
  <div class="field">
    <%= f.label :File %><br>
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
